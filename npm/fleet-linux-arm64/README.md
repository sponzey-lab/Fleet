# @sponzey/fleet-linux-arm64

Platform binary package for `@sponzey/fleet` on Linux arm64.

Release automation places the Rust `sponzey` binary at `bin/sponzey`.
The release workflow builds this package on Ubuntu 22.04 so the binary does not require glibc 2.39.

Licensed under `AGPL-3.0-only`. See the source repository for the complete
license text and corresponding source.
