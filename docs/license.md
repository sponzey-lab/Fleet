# License

Sponzey Fleet is distributed under the GNU Affero General Public License
version 3 only (`AGPL-3.0-only`). The complete license text is in the repository
root [`LICENSE`](../LICENSE) file.

This license declaration covers the Rust workspace, Web Admin source, npm
distribution wrapper, platform npm packages, and the distributed `sponzey`
binary unless an individual file explicitly states a different license.
Third-party dependencies remain subject to their respective licenses.

The npm packages and Cargo workspace must use the same SPDX identifier:

```text
AGPL-3.0-only
```

The npm package `license` field is registry metadata; it does not replace the
license text. Release checks reject package metadata that diverges from the
repository license declaration.
